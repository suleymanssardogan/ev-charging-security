1. terminal
cd /home/busra/Desktop/koordine_edilmis_talep_taraflı_saldırı
sudo modprobe vcan
sudo ip link add dev vcan0 type vcan
sudo ip link set up vcan0
candump vcan0

2.terminal
cd /home/busra/Desktop/koordine_edilmis_talep_taraflı_saldırı_ilk
source venv/bin/activate
python3 simulators/csms_sim.py

3. terminal
Komut,Açıklama
cd /home/busra/Desktop/koordine_edilmis_talep_taraflı_saldırı_ilk
source venv/bin/activate
python3 tests/run_ddos_attack.py
Eylem,CSMS başlatıldıktan sonra devam etmek için ENTER tuşuna basın... mesajını görünce ENTER tuşuna basın.
